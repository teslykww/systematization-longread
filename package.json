{
  "name": "systematization-longread",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/deploy-ftp.mjs",
    "deploy:1": "npm run build && node scripts/deploy-ftp.mjs 1",
    "deploy:2": "npm run build && node scripts/deploy-ftp.mjs 2",
    "deploy:subdomains": "npm run build && node scripts/deploy-ftp.mjs subdomains",
    "deploy:all": "npm run build && node scripts/deploy-ftp.mjs all"
  },
  "dependencies": {
    "framer-motion": "^12.34.0",
    "lucide-react": "^0.563.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
